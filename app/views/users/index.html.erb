<%= render 'books/list', user: @user, book: @book %>

=begin
<h1>Users</h1>
  <%= @users.each do |user| %>

  <p>image</p>
  <span><%= user.image %></span>

  <p>name</p>
  <span><%= user.name %></span>

  <% end %>
=end